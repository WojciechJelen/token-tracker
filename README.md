This is a Koii Assignmwent App.

## To run the project do the following

Install all dependencies (I recoomend `npm` instead of `yarn` to avoid conflicts)

```bash
npm install
```

and run the development server:

```bash
npm run dev
```
